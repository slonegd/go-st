1 ошибка при обращении к неизвестной переменной. Сейчас паника
2 попросить deepseek написать реализацию рекурсивной функции
3 в цикле for имена переменных при присваивании не работаеют из за того, что везде прокидывается контекст по указателю,
  а имя в этом контексте меняется. Надо имя положить в оператор